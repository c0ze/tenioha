; A pure recursive function with a declared result type.
関数 階乗 (数: 整数) を -> 整数 {
    もし (数 と 0 が 等しい) なら {
        1
    } そうでなければ {
        (数 に ((数 から 1 を 引く) を 階乗) を 掛ける)
    }
}

答え は (6 を 階乗)。
((答え を 文字列にする) を 表示する)。
